# Free API key from https://www.themoviedb.org/settings/api
# Enables the live "Netflix India" catalog (TMDB tracks Netflix IN daily).
# Without it the app uses the bundled catalog data.
EXPO_PUBLIC_TMDB_API_KEY=
